# AI Agent Memory v3-Expansion Index

**Authors: Joysusy & Violet Klaudia**

## Purpose

Canonical navigation entrypoint for the staged module pool under `developer-tool/ai-agent-memory/v3-expansion/`.

This document does **not** turn `v3-expansion/` into the memory lane's source of truth.
Instead, it provides a controlled way to navigate this staging layer without confusing it with:

- the canonical doctrine tree under `references/ai-agent-memory/`
- the runtime automation shell under `plugins/violet-memory-lab/`
- raw donor/source reservoirs outside the engine

Use this index only when the canonical memory references have already been consulted and a staged module cluster is still needed.

## Source Provenance

- **Primary source:** current `developer-tool/ai-agent-memory/v3-expansion/` module pool
- **Derived from:** memory-lane staging-governance formalization and cluster classification work
- **Upstream URL:** not applicable as a synthesized local staging index
- **Freshness status:** canonical local staging index aligned to the current ai-agent-memory control plane

---

## Rule

`v3-expansion/` is a staging and runtime-aligned module pool.
It is not the canonical doctrine tree.

The reading order remains:

1. `../README.md`
2. `../INVENTORY.md`
3. `../../references/ai-agent-memory/INDEX.md`
4. canonical memory references
5. **only then** this `v3-expansion/INDEX.md` when the canonical lane is insufficient

This index exists to reduce ambiguity inside staging, not to bypass doctrine.

---

## Cluster Navigation

Before entering a concrete module, prefer these staged cluster docs when possible:

- `MEMORY_CREATION_SHAPING_CLUSTER.md`
- `SESSION_HISTORY_RETRIEVAL_CLUSTER.md`
- `CONTINUITY_HYGIENE_CLUSTER.md`
- `SEARCH_SUBSTRATE_CLUSTER.md`

### 1. Memory creation / shaping

Use these when the problem is about creating, reshaping, or evaluating memory structures rather than ordinary recall or continuity retrieval.

| Module                           | Use When                                                                                   |
| -------------------------------- | ------------------------------------------------------------------------------------------ |
| `advanced-memory-skill-creator/` | creating or refining memory-oriented skills or memory-specific knowledge artifacts         |
| `agent-memory-skills/`           | reviewing memory-oriented skill-pack posture at a high level                               |
| `memory-systems/`                | browsing staged memory-system implementation and structure ideas beyond the canonical refs |
| `memory-retrieval-learning/`     | exploring retrieval-learning methodology or evaluation-oriented memory shaping             |

### 2. Session / history retrieval

Use these only after `references/ai-agent-memory/history-retrieval-patterns.md` has already selected the correct retrieval lane.

| Module                              | Use When                                              |
| ----------------------------------- | ----------------------------------------------------- |
| `cc-history/`                       | Claude Code session-history exploration               |
| `chat-history/`                     | exported or chat-style history extraction             |
| `claude-code-history-files-finder/` | deleted/overwritten file recovery from session traces |
| `code-history/`                     | code-evolution and git-history oriented questions     |
| `history-insight/`                  | structured session-history summarization              |
| `remembering-conversations/`        | conversation-recall oriented utilities                |
| `cass-memory/`                      | CASS-oriented memory retrieval support                |

### 3. Continuity / hygiene

Use these when the canonical continuity docs point you toward operational support or maintenance-style module behavior.

| Module                     | Use When                                                             |
| -------------------------- | -------------------------------------------------------------------- |
| `goodvibes-memory/`        | schema- or memory-usage support utilities are needed                 |
| `memory-hygiene/`          | memory cleanup and hygiene behavior is the real concern              |
| `memory-model/`            | low-level memory-model concepts or specialized references are needed |
| `meta-cognition-parallel/` | parallel/meta-cognitive support patterns are relevant                |

### 4. Search substrate

Use only when the canonical retrieval doctrine explicitly requires a heavy indexed substrate.

| Module                              | Use When                                                                                        |
| ----------------------------------- | ----------------------------------------------------------------------------------------------- |
| `coding_agent_session_search-main/` | cross-source, indexed, high-volume retrieval is required and the lighter lanes are insufficient |

---

## Preferred Reading Paths by Need

### If the question is about recall doctrine

Start here instead:

- `../../references/ai-agent-memory/recall-before-act.md`
- `../../references/ai-agent-memory/claude-recall-operating-model.md`
- `../../references/ai-agent-memory/claude-recall-patterns.md`

### If the question is about continuity or interruption recovery

Start here instead:

- `../../references/ai-agent-memory/continuity-control-plane.md`
- `../../references/ai-agent-memory/working-state-and-self-reminder-discipline.md`

### If the question is about retrieval lane selection

Start here instead:

- `../../references/ai-agent-memory/history-retrieval-patterns.md`

Only enter the staged modules after those docs tell you a deeper operational cluster is needed.

---

## What This Index Prevents

This index exists to prevent the following mistakes:

- treating all staged modules as equally canonical
- entering `coding_agent_session_search-main/` before a retrieval lane has been chosen
- using skill packs as doctrine substitutes
- confusing continuity support modules with recall doctrine
- mistaking adapted module pools for cleanup-ready canonical structure

---

## Cross-Links

Read this alongside:

- `README.md`
- `README.zh-CN.md`
- `MEMORY_CREATION_SHAPING_CLUSTER.md`
- `SESSION_HISTORY_RETRIEVAL_CLUSTER.md`
- `CONTINUITY_HYGIENE_CLUSTER.md`
- `SEARCH_SUBSTRATE_CLUSTER.md`
- `MODULE_STATUS_MATRIX.md`
- `MODULE_STATUS_MATRIX.zh-CN.md`
- `../README.md`
- `../INVENTORY.md`
- `../../references/ai-agent-memory/INDEX.md`
- `../../references/ai-agent-memory/history-retrieval-patterns.md`
- `../../references/ai-agent-memory/source-reservoir-map.md`

---

## Final Doctrine

The reusable lesson is not:

> “this index tells us which staged memory module to open.”

The reusable lesson is:

> “staging layers still need disciplined navigation: classify the need first through canonical doctrine, then enter the smallest staged cluster that adds real leverage, so adapted module pools remain useful without pretending to be the engine's first source of truth.”
