---
description: Prime the language-specialists control plane inside developer-tool
allowed-tools: Read, Grep, Glob
---

# Prime Language Specialists Surface

1. Read `skills/language-specialists/SKILL.md`.
2. Then read `language-specialists/README.md` to recover the subsystem's doctrine-first bridge model.
3. Read `language-specialists/INVENTORY.md` if maturity or layer boundaries are still unclear.
4. Read `language-specialists/TRIGGER_SCOPE.md` if first-owner routing is part of the problem.
5. Distinguish clearly between:
   - subtree control plane
   - canonical doctrine under `references/language-specialists/`
   - root runtime surfaces in `developer-tool`
   - donor/source fallback
6. Route the task to one primary layer first.
7. Name the primary layer, one secondary layer if needed, and the next exact file to read.
8. Do not invent a dedicated subsystem runtime shell or active staging behavior unless doctrine/control-plane pressure explicitly justifies it.
