---
description: Prime the memory and continuity control plane inside developer-tool
allowed-tools: Read, Grep, Glob
---

Prime the memory domain inside `developer-tool`.

1. Read `skills/ai-agent-memory/SKILL.md`.
2. Then read `ai-agent-memory/README.md` to recover the current four-layer control model.
3. If routing remains fuzzy, read `ai-agent-memory/TRIGGER_SCOPE.md`.
4. Distinguish clearly between:
   - canonical doctrine in `references/ai-agent-memory/`
   - staged module clusters in `ai-agent-memory/v3-expansion/`
   - push-based runtime automation in `plugins/violet-memory-lab/`
   - donor/source reservoirs used only as fallback evidence
5. Route the task to one primary layer first.
6. If the runtime layer is primary, also read `../../plugins/violet-memory-lab/README.md` and the relevant shell command surface (`init-continuity`, `capture-continuity`, `recover-context`, or `trace-continuity`).
7. Name the primary layer, one secondary layer if needed, and the next exact file to read.
8. Do not treat staging or donor material as first-line doctrine by default.
