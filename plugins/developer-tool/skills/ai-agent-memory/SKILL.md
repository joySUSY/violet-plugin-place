---
name: ai-agent-memory
description: Use when the task involves recall, continuity, prior-session recovery, history retrieval, trigger ownership, staging-layer memory modules, or memory hygiene inside developer-tool.
---

# AI Agent Memory

## Steps

1. Read `../../ai-agent-memory/README.md`.
2. Read `../../ai-agent-memory/INVENTORY.md` if you need the current layer model first.
3. Read `../../ai-agent-memory/TRIGGER_SCOPE.md` if first-owner routing is unclear.
4. Read `../../references/memory-systems-overview.md` if you need the top-level bridge doctrine first.
5. Enter `../../references/ai-agent-memory/INDEX.md` for the canonical doctrine tree.
6. Only enter `../../ai-agent-memory/v3-expansion/INDEX.md` after canonical doctrine is insufficient.
7. Enter `../../../plugins/violet-memory-lab/README.md` only when the problem becomes push-based and operational.
8. Use `../../../plugins/violet-memory-lab/commands/init-continuity.md`, `capture-continuity.md`, `recover-context.md`, or `trace-continuity.md` only after the runtime layer is clearly primary.
9. Treat donor families as fallback evidence only after doctrine + staging are insufficient.

## Core Rule

This bridge skill exists to make the matured memory control plane discoverable from the plugin shell.
It is a routing surface, not a second knowledge center and not a shortcut around the canonical references tree.
