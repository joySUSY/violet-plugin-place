#!/usr/bin/env bash
set -euo pipefail
cat >/dev/null 2>/dev/null || true
printf '%s\n' '{"continue":true,"suppressOutput":true,"systemMessage":"Before compaction, preserve which developer-tool surface was active, what runtime shell files were touched, and whether work stayed within additive-only boundaries. Do not allow compaction to erase the distinction between doctrine, runtime shell, and donor reservoirs."}'
