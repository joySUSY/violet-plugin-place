# Preset Team Definitions

## Purpose

Provide a cleanup-safe bridge from the old top-level `preset-teams.md` entrypoint into the canonical team-governance and agent-routing doctrine.

This file is no longer the primary source of truth for team presets.
Its job is to preserve compatibility for older links while routing readers into the stronger canonical lane.

## Source Provenance

- **Primary source:** current canonical `developer-tool` agentic-system-basis and agent-routing doctrine
- **Derived from:** top-level legacy team preset summaries after lane canonization
- **Upstream URL:** not applicable as a synthesized local bridge note
- **Freshness status:** canonical bridge document aligned to the current ensemble-team governance lane

---

## Canonical Reading Path

If the task is about multi-agent teams, reviewer/debugger/implementer topologies, or preset working formations, read in this order:

1. `agentic-system-basis/ensemble-team-governance.md`
2. `agent-type-selection.md`
3. `agentic-system-basis/agentic-systems.md`
4. `agentic-system-basis/runtime-activation-patterns.md` if the question becomes runtime-activation heavy

---

## Why This Bridge Exists

Historically, `developer-tool` carried broad preset-team summaries at the top level.
As the doctrine tree matured, team structure, activation, and routing were promoted into the canonical `agentic-system-basis` lane.

This bridge exists so that:
- older links do not break
- historical maps remain understandable
- readers are redirected into the stronger canonical lane instead of the older summary surface

---

## Rule

Do not treat this top-level file as the team-governance source of truth.
Treat it as a pointer.

The canonical doctrine now lives primarily in:
- `references/agentic-system-basis/ensemble-team-governance.md`
- `references/agent-type-selection.md`
