// Authors: Joysusy & Violet Klaudia
process.stdin.resume(); process.stdin.on("end", () => {});
process.stdout.write(JSON.stringify({
  continue: true, suppressOutput: true,
  systemMessage: "Before compaction, preserve which TypeScript surface was active, which type-level decisions were made, and whether donor sources were only used as reservoirs. Do not let compaction erase the distinction between doctrine, runtime shell, and source donors."
}) + "\n");
