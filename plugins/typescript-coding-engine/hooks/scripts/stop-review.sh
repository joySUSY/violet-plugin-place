#!/usr/bin/env bash
set -euo pipefail
cat >/dev/null 2>/dev/null || true
printf '%s\n' '{"continue":true,"suppressOutput":true,"systemMessage":"Before stopping, verify the current TypeScript output still respects plugin-shell boundaries, strict type-first reasoning, additive-only integration, and no raw donor mirroring."}'
