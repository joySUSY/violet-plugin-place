#!/usr/bin/env bash
set -euo pipefail
cat >/dev/null 2>/dev/null || true
printf '%s\n' '{"continue":true,"suppressOutput":true,"systemMessage":"Before compaction, preserve which TypeScript surface was active, what type/runtime/tooling decisions were made, and whether donor material was only used as a reservoir. Do not let compaction erase the distinction between doctrine, runtime shell, and source donors."}'
