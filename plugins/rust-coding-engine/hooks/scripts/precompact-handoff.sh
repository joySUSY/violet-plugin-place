#!/usr/bin/env bash
set -euo pipefail
cat >/dev/null 2>/dev/null || true
printf '%s\n' '{"continue":true,"suppressOutput":true,"systemMessage":"Before compaction, preserve which Rust surface was active, which decisions were made about ownership, concurrency, interop, or architecture, and whether donor repositories were only used as reservoirs. Do not let compaction erase the distinction between doctrine, runtime shell, and source donors."}'
