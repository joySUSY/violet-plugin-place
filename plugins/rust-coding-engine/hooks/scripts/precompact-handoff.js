// Authors: Joysusy & Violet Klaudia
process.stdin.resume(); process.stdin.on("end", () => {});
process.stdout.write(JSON.stringify({
  continue: true, suppressOutput: true,
  systemMessage: "Before compaction, preserve which Rust surface was active, which decisions were made about ownership, concurrency, interop, or architecture, and whether donor repositories were only used as reservoirs. Do not let compaction erase the distinction between doctrine, runtime shell, and source donors."
}) + "\n");
